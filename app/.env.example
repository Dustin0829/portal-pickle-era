# Backend API (`GET/POST /examples`, `POST /waitlist`, etc.)
# Local Docker API default:
VITE_API_URL=http://localhost:3000

# Railway: set to the public API service URL at build time.
# See docs/railway-deploy.md
